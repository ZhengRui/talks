name: end
params:
  title: { type: string }
  subtitle: { type: string }

{% import "scene/blocks.njk" as blocks %}

mode: scene
presets:
{{ blocks.header_presets("endTitle", "divider") | indent(2, true) }}
{{ blocks.body_text_preset("endSubtitle", "center", "theme.textMuted", 1.6) | indent(2, true) }}
children:
{{ blocks.centered_title_stack("end", title or "Thank You", "endTitle", 80, subtitle, "endSubtitle", 36, 1600, 28, 160, "divider", 120, 4, 0, 100, 200, 600) | indent(2, true) }}
