---
title: 'Advanced MDX Slide'
---
import SimpleComponent from './SimpleComponent'

<section data-background-color="aquamarine">

# {frontmatter.title}

This content is rendered server-side (or at build time) using `next-mdx-remote`.

It allows embedding React components:

<SimpleComponent message="Hello from MDX!" />

</section>
